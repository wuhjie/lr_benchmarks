# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local Claude settings
.claude/settings.local.json

# HuggingFace download cache
pasa-dataset/.cache/

# Large dataset blob — exceeds GitHub 100MB limit (2.3GB)
pasa-dataset/paper_database/cs_paper_2nd.zip

# LitSearch HF cache + large corpus parquet shards (>100MB each)
litsearch-dataset/.cache/
litsearch-dataset/corpus_clean/
litsearch-dataset/corpus_s2orc/

# Decrypted AutoResearchBench questions — upstream asks not to repost plaintext Q&A online
autoresearch/dataset/AutoResearchBench.jsonl
